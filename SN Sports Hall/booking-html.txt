<div class="panel panel-default">
  <div class="panel-heading"><h3>Book a Sports Hall</h3></div>
  <div class="panel-body">
    <div id="booking-form-anchor"></div>

    <div class="form-group">
      <label>Select Hall</label>
      <select class="form-control" ng-model="c.selectedHall" ng-change="c.loadSlots()">
        <option value="">-- Choose a Hall --</option>
        <option ng-repeat="hall in data.halls" value="{{hall.sys_id}}">{{hall.name}}</option>
      </select>
    </div>

    <div class="form-group" ng-if="data.slots.length > 0">
      <label>Select Available Slot</label>
      <select class="form-control" ng-model="c.selectedSlot">
        <option value="">-- Choose a Slot --</option>
        <option ng-repeat="slot in data.slots" value="{{slot.sys_id}}">
          {{slot.booking_date}} | {{slot.start_time}} - {{slot.end_time}}
        </option>
      </select>
    </div>

    <button class="btn btn-primary" ng-click="c.submitBooking()" ng-disabled="!c.selectedSlot">
      Submit Booking Request
    </button>

    <p class="text-success" ng-if="c.message">{{c.message}}</p>

  </div>
</div>